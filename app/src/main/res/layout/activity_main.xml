<?xml version="1.0" encoding="UTF-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ffffff"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/main_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="46dp"
        android:gravity="center"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:layout_gravity="bottom"
        android:layout_marginBottom="45dp"
        android:background="#afafaf" />

    <RadioGroup
        android:id="@+id/rg"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:layout_gravity="bottom"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/rbHome"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="#FFFFFF"
            android:button="@null"
            android:drawableTop="@drawable/select_home_check_bg"
            android:gravity="center"
            android:paddingTop="3dp"
            android:text="首页"
            android:textColor="@drawable/selector_main_text"
            android:textSize="14sp" />

        <RadioButton
            android:id="@+id/rbbianmin"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="#FFFFFF"
            android:button="@null"
            android:drawableTop="@drawable/select_bianmin_check_bg"
            android:gravity="center"
            android:paddingTop="3dp"
            android:text="便民服务"
            android:textColor="@drawable/selector_main_text"
            android:textSize="14sp" />

        <RadioButton
            android:id="@+id/rbxunjian"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="#FFFFFF"
            android:button="@null"
            android:drawableTop="@drawable/select_patrol_check_bg"
            android:gravity="center"
            android:paddingTop="3dp"
            android:text="巡检"
            android:textColor="@drawable/selector_main_text"
            android:textSize="14sp"
            android:visibility="gone" />

        <RadioButton
            android:id="@+id/rbMy"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="#FFFFFF"
            android:button="@null"
            android:drawableTop="@drawable/select_my_check_bg"
            android:gravity="center"
            android:paddingTop="3dp"
            android:text="个人中心"
            android:textColor="@drawable/selector_main_text"
            android:textSize="14sp" />
    </RadioGroup>

</FrameLayout>